# Discución_clase_16_Max

## 1. Diferencia entre algoritmo correcto y función robusta.

La diferencias principales entre ambas es que la implementación robusta lo que hace es que limita los errores, entonces puedes correr los códigod sin tener problemas como los que llegamos a tener en la impelentacón de la clase pasada.

## 2. Razón de separar normalización.

la razón para hacerlo es para poderlo correr de manera separada y no de manera conjunta, esto nos puede ayudar dependiendo de lo que queramos hacer y dependiendo de si lo queremos hacer más especifico o menos.

## 3. Mapping/Sequence frente a dict/list.

La mayor ventaja es a la hora de hacer cosas más abstractas, ya que si lo hacemos directamente con diccionarios y listas tenemos que especificar bien las cosas y hacerlo con mapping y sequence no hay que hacerlo.

## 4. TypeError frente a ValueError.

Esto nos sirve principalmente para saber donde se rompe el código, para hacer la explicacipón simpke, es una sección para poder analizar donde es que se rompoe el código y así hacer las correcciones correspondientes, ojo que los errores no seran del código perce, si no a la hora de implementar los datos.

## 5. Bool, NaN e infinito.

Lo que tiene el nan es que se tiene que analizar aparte para poder ver los casos especificos, ya que de no haerlo va a saltar errores donde puede que no loa haya, es por una de esas razones por las cuales creamos la función normalizar.

## 6. Copia defensiva.

La copia defensiva son los datos que agregamos inicialmente sin necesisdade de normalizarlos, y esto es importante, porque así podemos ver cuales fdueron los datos originales.

## 7. Vecino implícito.

Los vecinos implicitos quiere decir los vecinos que sabmos que de una manera u otra estan conectados con los demas, entonces esto es importante porque así podemos leer entre lineas entre ambas implementaciones.

## 8. Invariante de entradas obsoletas.

Lo invariante en este algoritmo es el punto de partida o origen del cual vamos a hacer todo lo demas, entonces esto va a dictaminar todo lo demas.

## 9. Responsabilidades de reconstrucción.

Hay que ser responsables a la hora de reconstruir o normalizar los datos ingresados, porque de no estar consientes de ello podemos por accidente obtener cosas que no queiramos de buenas a primeras.

## 10. Matriz contrato–riesgo–prueba.

Lo que hace principalmente esta matriz es que verifica que todo se haya implementado de manera correcta, o sea que se normalizen las cosas para que no aparezcanerrores y que esta normalización sea guardada de manera correcta.

## 11. Complejidad de normalización y Dijkstra.

La complejidad no la acanbe de entender por completo, pero por lo que llegue a entender es que no cambia del todo  pero esta normalización la simplifica un poco.

## 12. Operación dominante en BFS, Dijkstra, Kruskal y topológico.

La funcion dominante viene siendo la BFS porque de ahí se desprenden las demas, por lo que es la dominante.