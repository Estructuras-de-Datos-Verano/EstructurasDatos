# 1
Aristeo
# 2
Un TDA es un contrato que describe su comportamiento, operaciones, casos y detalles que no pertenecen a la interfaz 
# 3
La interfaz es la parte visible de una funcion y la implementacion es el como funciona.
# 4
Para validar comillas en una cita textual directa dentro de un escrito
# 5
push(elemento): agrega un elemento al tope de la pila.
pop(): quita y devuelve el elemento del tope.
peek(): devuelve el elemento del tope sin quitarlo.
vacia(): indica si la pila está vacía.
size(): devuelve la cantidad de elementos en la pila.
# 6
Dado una pila vacía cuando se inserta un elemento x con push y luego se saca con pop, entonces el elemento devuelto debe ser A
y la pila debe quedar vacía (vacia() es true).